% Per-study training recipes used throughout the thesis.
\begin{table}[ht]
    \centering
    \caption[Training recipe: LAW (cross-city benchmark).]{Training recipe for the LAW agent in the cross-city benchmark's open-loop nuScenes evaluation. Every LAW entry in the cross-city tables uses these settings unchanged; only the training-city filter and random seed vary across runs. AdamW with a cosine schedule and a comparatively large gradient-clip norm are both inherited from the LAW reference implementation. A single L40S GPU is sufficient at batch size $2$; the recipe is otherwise unchanged from~\cite{li-law-2024}.}
    \label{tab:hparam-phase1-law}
    \small
    \begin{tabular}{ll}
    \toprule
    Optimizer             & AdamW \\
    Learning rate         & $5\times10^{-5}$ \\
    Weight decay          & $0.01$ \\
    Scheduler             & Cosine, no warm-up \\
    Batch size            & 2 (effective) \\
    Epochs                & 12 \\
    Gradient clip         & 35 \\
    GPUs                  & 1 $\times$ L40S \\
    Precision             & 16-mixed \\
    \bottomrule
    \end{tabular}
\end{table}

\begin{table}[ht]
    \centering
    \caption[Training recipe: TransFuser / Latent TransFuser.]{Training recipe for every TransFuser and Latent TransFuser row behind the v1.1 cross-city matrix in~\reftab{tab:navsim_pdms_merged_v11}. The recipe reproduces the original TransFuser paper exactly: plain Adam (not AdamW), no weight decay, no learning-rate schedule, and gradient clipping at $1.0$. Deviations from this recipe --- adding AdamW, weight decay, or a cosine schedule --- silently regress all-city PDMS by more than thirty percentage points with no explicit error, which is why the recipe is documented in full here rather than abbreviated.}
    \label{tab:hparam-phase1-tf}
    \small
    \begin{tabular}{ll}
    \toprule
    Optimizer             & plain Adam (\textbf{not} AdamW) \\
    Learning rate         & $1\times10^{-4}$ \\
    Weight decay          & \textbf{0} (no decay) \\
    Scheduler             & \textbf{None} (constant lr) \\
    Batch size            & 32/GPU, effective 128 \\
    Epochs                & 20 (paper baseline) or 100 (full) \\
    Gradient clip         & 1.0 \\
    LiDAR backbone        & ResNet-34 (\textbf{not} ResNet-18) \\
    GPUs                  & 4 $\times$ H200 \\
    Precision             & 16-mixed \\
    \bottomrule
    \end{tabular}
\end{table}

\begin{table}[ht]
    \centering
    \caption[Training recipe: DiffusionDrive + SSL (generative-planning study).]{Training recipe for the DiffusionDrive$+$SSL rows of the generative-planning study (\reftab{tab:diffusiondrive-allcity}). The image encoder is frozen by default and the trajectory head is initialised from a $256$-anchor K-means bank fitted on \texttt{navtrain} trajectories; at inference the head runs two truncated denoising steps. Every other optimisation setting mirrors the base DiffusionDrive recipe~\cite{jia-diffusiondrive-2025}, so differences between rows in~\reftab{tab:diffusiondrive-allcity} are attributable to the image encoder alone.}
    \label{tab:hparam-phase2-dd}
    \small
    \begin{tabular}{ll}
    \toprule
    Optimizer             & Adam \\
    Learning rate         & $1\times10^{-4}$ \\
    Weight decay          & $0$ \\
    Scheduler             & Constant \\
    Batch size            & 32/GPU, effective 128 \\
    Epochs                & 20 \\
    K-means anchor count  & 256 \\
    Denoising steps       & 2 (truncated) \\
    GPUs                  & 2 $\times$ H200 \\
    Precision             & 16-mixed \\
    \bottomrule
    \end{tabular}
\end{table}

\begin{table}[ht]
    \centering
    \caption[Pre-training recipe: I-JEPA ViT-S/14 on nuScenes.]{Domain-specific self-supervised pre-training recipe for I-JEPA ViT-S/14 on nuScenes front-camera frames. The resulting checkpoints are used wherever the main text refers to ``nuScenes rect.'' or ``nuScenes sq.'' I-JEPA variants in the cross-city tables (\reftab{tab:navsim_pdms_merged_v11}, \reftab{tab:navsim_pdms_merged}). The rectangular variant uses the native camera aspect ratio ($224 \times 392$); the square variant uses a centre-cropped $224 \times 224$ patch that matches the ImageNet pre-training geometry of the other SSL backbones. Identical recipes are used for the DINOv2 and MAE domain-specific variants with the objective swapped accordingly.}
    \label{tab:hparam-phase3-ssl-pretrain}
    \small
    \begin{tabular}{ll}
    \toprule
    Architecture          & ViT-S/14, 22M parameters \\
    Objective             & I-JEPA target-vs-context prediction \\
    Optimizer             & AdamW \\
    Learning rate         & Cosine schedule, peak $5\times10^{-4}$ \\
    Weight decay          & $0.04$ \\
    Epochs                & 100 \\
    Batch size            & 64/GPU, effective 128 \\
    Input resolution      & $224\times224$ (sq.) or $224\times392$ (rect.) \\
    Dataset               & nuScenes front-camera frames (340k images) \\
    GPUs                  & 2 $\times$ H200 \\
    Precision             & 16-mixed \\
    \bottomrule
    \end{tabular}
\end{table}
